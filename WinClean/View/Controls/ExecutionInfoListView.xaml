<UserControl
    x:Class="Scover.WinClean.View.Controls.ExecutionInfoListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Scover.WinClean.View.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:Scover.WinClean.Resources"
    xmlns:ui="clr-namespace:Scover.WinClean.Resources.UI"
    xmlns:viewModel="clr-namespace:Scover.WinClean.ViewModel"
    mc:Ignorable="d">
    <UserControl.ContentTemplate>
        <DataTemplate DataType="viewModel:ExecutionInfoListViewModel">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <ListView
                    Margin="0,0,10,0"
                    IsSynchronizedWithCurrentItem="True"
                    ItemsSource="{Binding ExecutionInfos}">
                    <ListView.View>
                        <GridView AllowsColumnReorder="False">
                            <GridViewColumn>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="viewModel:ExecutionInfoViewModel">
                                        <Image Width="16" Height="16">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Result.Succeeded}" Value="{x:Null}">
                                                            <Setter Property="Source" Value="{x:Null}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Result.Succeeded}" Value="False">
                                                            <Setter Property="Source" Value="{StaticResource Cross}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Result.Succeeded}" Value="True">
                                                            <Setter Property="Source" Value="{StaticResource Checkmark}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn DisplayMemberBinding="{Binding Script.Name}" Header="{x:Static res:Script.Name}" />
                            <GridViewColumn DisplayMemberBinding="{Binding Capability.Name}" Header="{x:Static ui:ExecutionInfoListView.Action}" />
                            <GridViewColumn DisplayMemberBinding="{Binding Result.ExitCode}" Header="{x:Static res:ExecutionResult.ExitCode}" />
                            <GridViewColumn DisplayMemberBinding="{Binding Result.FormattedExecutionTime}" Header="{x:Static res:ExecutionResult.ExecutionTime}" />
                        </GridView>
                    </ListView.View>
                </ListView>
                <GridSplitter
                    Grid.RowSpan="3"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    Background="{x:Static SystemColors.ControlLightBrush}" />
                <controls:ExecutionInfoView
                    Grid.Column="2"
                    Margin="10,0,0,0"
                    Content="{Binding CurrentItem}" />
            </Grid>
        </DataTemplate>
    </UserControl.ContentTemplate>
</UserControl>
